// Ensure the full i18n data is loaded (i18n-data.js sets window.__voyageI18N)
import './i18n-data.js';

// Bootstrap module: only preload I18N data.
// The main app (src/js/app.js) contains the `boot()` function and invokes it at the end.

